# BEACON - network monitor

Simple web dashboard for monitoring devices on a local network (ping / HTTP / TCP port). Install with `npm install`, run `node setup.js` once and then start it with `npm start`. The app is available at http://localhost:3000.
